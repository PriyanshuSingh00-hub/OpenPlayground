name: Auto Comment on Issue

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: Add Comment to Issue
        uses: actions/github-script@v6
        with:
          script: |
            const issueNumber = context.issue.number;

            const commentBody1 = `
### Thank you for raising this issue! 🙌
We'll review it as soon as possible. We truly appreciate your contribution! ✨

> 📌 **Before moving ahead:**
> - Please make sure you've gone through **README.md**, **CONTRIBUTING.md**, and **CODE_OF_CONDUCT.md**.
> - 🚫 Do **NOT** create a PR until this issue has been **assigned to you**.

---

### 🌟 Support & Explore
If you like this repository, don’t forget to **⭐ star it** — it really helps and motivates maintainers!

Also, check out **Linkshala** 🚀 for:
- 🛠️ New project ideas  
- 📚 Learning resources  
- 🎮 Fun games & experiments  

It’s a great place to explore and build something amazing. Happy contributing! 😊
`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: commentBody1
            });

            console.log('Comment added successfully.');
